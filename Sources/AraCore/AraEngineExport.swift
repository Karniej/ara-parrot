// AraCore is the macOS assembly of the portable engine; every type that used
// to live here and now lives in AraEngine is re-exported so the executable,
// the tests and any downstream import of AraCore see one unchanged surface.
@_exported import AraEngine
